{
  "name": "pipedrive-ops-mcp",
  "version": "1.0.0",
  "description": "Read-mostly MCP server over the Pipedrive API v2, plus an n8n daily-digest agent that uses it",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build:workflow": "node scripts/build-workflow.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.6.5"
  }
}
